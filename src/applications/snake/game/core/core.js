export default class GameCore {
  constructor(scene) {
    this.scene = scene;
  }

  start() {
    this.scene.start();
  }

  pause() {
    // this.scene.pause();
  }

  resume() {
    // this.scene.resume();
  }

  destroy() {
    // this.scene.destroy();
  }
}
