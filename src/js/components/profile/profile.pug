- const userName = 'Dieter Rams'
.block#profile
  img(src = 'https://wavebackend.now.sh/' + user.avatarSource)
  h3= user.username
  h3= user.score
  a.button(
    datahref='editprofile'
  ) Edit
  a.button(
    datahref='menu'
  ) Back
