- const userName = 'Dieter Rams'
.block#profile
  img(src = user.avatarSource)
  h3= user.username
  h3= user.score
  a.button(
    datahref='editprofile'
  ) Редактировать
  a.button(
    datahref='menu'
  ) Назад
